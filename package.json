{
  "name": "mldock",
  "description": "NodeJS/TypeScript library/CLI -- downloads MarkLogic rpms and builds docker images hosting MarkLogic Server 8+",
  "keywords": [
    "marklogic",
    "docker"
  ],
  "author": "Daphne Maddox <laurelnaiad@gmail.com>",
  "publisher": "laurelnaiad",
  "license": "MIT",
  "version": "0.0.6",
  "main": "index.js",
  "types": "index.d.ts",
  "bin": {
    "mldock": "cli/cli.js",
    "mldock-download": "cli/cli-download.js",
    "mldock-build": "cli/cli-build.js"
  },
  "bugs": {
    "url": "https://github.com/laurelnaiad/mldock/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/laurelnaiad/mldock.git"
  },
  "engines": {
    "npm": ">=2.0.0",
    "node": ">= 6.5.0"
  },
  "dependencies": {
    "@types/dockerode": "^2.4.5",
    "chalk": "^2.1.0",
    "commander": "^2.11.0",
    "dockerode": "^2.5.0",
    "fs-extra": "^4.0.1",
    "got": "laurelnaiad/got#master",
    "log-update": "^2.1.0",
    "tar-fs": "^1.15.3",
    "through2": "^2.0.3",
    "tough-cookie": "^2.3.2"
  },
  "scripts-info": {
    "info": "list the high-level npm scripts",
    "build": "build src and test to build directory, copy dockerfiles to build directory",
    "test": "run tests and report code coverage into the coverage directory",
    "dist": "build src to dist directory, copy dockerfiles to dist directory, make package content in dist directory",
    "cleanup": "rm -rf the build, and dist and coverage directories"
  },
  "scripts": {
    "info": "npm-scripts-info",
    "build": "tsc -p . && shx mkdir -p build/src/dockerFiles && shx cp -R src/dockerFiles/* build/src/dockerFiles/",
    "test": "npm-run-all clean_build build && node_modules/.bin/nyc node_modules/.bin/mocha build/test/index.unit.js",
    "dist": "tsc -p src && shx mkdir -p dist/dockerFiles && shx cp -R src/dockerFiles/* dist/dockerFiles/ && node scripts/makePackage.js",
    "cleanup": "npm-run-all clean_build clean_cover",
    "clean_build": "shx rm -rf build",
    "clean_cover": "shx rm -rf coverage",
    "clean_dist": "shx rm -rf dist",
    "preversion": "npm run test",
    "version": "npm-run-all clean_dist dist",
    "version_patch": "npm version patch"
  },
  "devDependencies": {
    "@types/chai": "^4.0.3",
    "@types/fs-extra": "^4.0.0",
    "@types/got": "^7.1.2",
    "@types/mocha": "^2.2.41",
    "@types/sinon": "^2.3.3",
    "@types/tough-cookie": "^2.3.1",
    "chai": "^4.1.1",
    "codecov": "^2.3.0",
    "dotenv": "^4.0.0",
    "mocha": "^3.5.0",
    "npm-run-all": "^4.0.2",
    "npm-scripts-info": "^0.3.6",
    "nyc": "^11.1.0",
    "shx": "^0.2.2",
    "sinon": "^3.2.1",
    "source-map-support": "^0.4.16",
    "typescript": "^2.4.2"
  },
  "nyc": {
    "timeout": 10000,
    "include": [
      "**/build/src/**/*.js"
    ],
    "reporter": [
      "text",
      "lcov",
      "html"
    ],
    "require": [
      "source-map-support/register"
    ],
    "sourceMap": true
  }
}
